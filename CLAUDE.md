# seat-randomizer — 컴퓨터실 자리뽑기

## ① 앱의 목적과 대상

다른 레포들(postal-simulator, data-cleaner, ai-explorer 등)이 **학생이 직접 쓰는 수업용 앱**인 것과
달리, 이 앱은 **교사가 컴퓨터실 자리 배치를 위해 쓰는 도구**다. 특정 단원·성취기준에 묶여 있지 않고,
컴퓨터실을 쓰는 어떤 수업에서든 매 시간 반복되는 "자리를 무작위로, 그러나 몇 자리는 고정해서 앉히는"
일을 대신해 준다.

- **대상**: 교사 (학생에게 보여줄 좌석표를 만들고 인쇄/저장하기 위한 용도)
- **입력**: 날짜 · 교시 · 학년/반 · 학생 인원수
- **출력**: 좌석 배치도 2종 (학생이 보는 화면 / 교사가 보는 화면), PNG로 저장 가능

## ② 기능 목록

### 좌석 배치 편집

- 컴퓨터실을 **구역(N개) × 행 × 열**로 정의한다. 구역마다 행·열 수가 달라도 되고(예: 큰 구역은
  5행 2열, 작은 구역은 2행 2열), 구역 순서를 좌우로 옮길 수 있다(◀▶).
- 자리를 클릭하면:
  - **고정 배정 번호** 지정 — 이 자리에는 항상 특정 출석번호 학생이 앉는다(무작위 배정에서 제외).
  - **사용 중지** — 컴퓨터 고장 등으로 못 쓰는 자리를 배정 대상에서 뺀다(배치도에는 빗금으로 계속 표시).
- 좌석 배치(구역 구성 + 사용 중지 목록)는 **반과 무관하게 공용**으로 저장된다 — 컴퓨터실 자체의
  물리적 구조이기 때문이다.
- **고정석과 학생 인원수는 학년/반별로 따로** 저장된다. 예: "1학년 1반, 20명"을 한 번 저장해 두면
  다음에 학년을 1, 반을 1로 선택하는 순간 인원수 칸이 자동으로 20으로 채워진다.
- **좌석 배치 편집을 닫는 순간** 바뀐 행/열이 아래 결과 화면(학생/교사가 보는 화면)에 바로 반영된다
  — 무작위 배정을 눌러야만 확인되는 게 아니라, 닫자마자 배치가 바뀐 걸 눈으로 볼 수 있다(번호 자체는
  다시 섞이지 않고, 사라진 자리는 그냥 없어지고 새로 생긴 자리는 "-"로 비어 있는 채로 보인다).

### 반별 학생수 미리 설정

- 학년(1~3) × 반(1~9) 표에 인원수를 한 번에 입력해 두는 화면. 실제로 있는 반만 채우면 된다
  (예: 1학년이 8개 반이면 9반 칸은 비워 둔다).
- **비워둔 칸은 손대지 않는다** — 이미 저장돼 있던 값이 있으면 그대로 남는다. 저장을 누르면 값이 있는
  칸만 해당 반의 학생 인원수로 반영된다.
- 위 학년/반 드롭다운에서 그 반을 고르면 여기서 설정한 인원수가 자동으로 채워진다(같은 저장소를
  공유하므로, 이 표를 안 쓰고 직접 반을 골라 인원수를 입력해도 동일하게 저장된다).

### 무작위 배정

- 인원수만큼 출석번호를 셔플해서, 고정석을 제외한 나머지 빈 자리에 무작위로 채운다.
- 좌석 수보다 인원수가 많으면 에러 배너(스크롤 자동 이동), 고정 번호가 서로 겹치면 토스트로 경고하고
  뒤쪽 중복은 무시한다.
- 좌석이 인원수보다 많이 남으면 남는 자리는 **공석**으로 무작위 위치에 표시된다(항상 뒷줄이 비는 게
  아니라, 빈자리도 셔플 대상에 넣는다).
- 버튼을 누르면 바로 바뀌지 않는다. 화면 한가운데에 **5·4·3·2·1 카운트다운**이 뜬 다음 실제로
  배정이 바뀌고, 그 순간 **색종이(컨페티)**가 화면 전체에 떨어진다. 카운트다운 중에는 버튼이
  비활성화되어 중복 클릭으로 두 카운트다운이 겹치지 않는다.
- 컨페티는 `requestAnimationFrame`이 아니라 `setInterval`로 움직인다. 브라우저는 탭이 화면에 실제로
  보이지 않는 동안 rAF를 멈추기 때문에, 창이 잠깐 포커스를 잃어도 효과가 얼어붙거나 건너뛰지 않고
  끝까지 재생되게 하려는 의도다.

### 학생이 보는 화면 / 교사가 보는 화면 (탭 전환)

같은 배정 결과를 **두 가지 방향으로** 그린다 — 좌우만 뒤집는 게 아니라 **180도 통째로 돌린다**:

| | 학생이 보는 화면 | 교사가 보는 화면 |
|:---|:---|:---|
| 칠판 위치 | 위쪽 (칠판을 마주 볼 때 기준) | **아래쪽** (교사가 칠판을 등지고 학생을 마주 볼 때 기준) |
| 구역·행·열 순서 | 그대로 | 구역 순서·열 순서·행 순서를 모두 반전 |
| 행 수가 다른 구역 정렬 | 칠판 쪽(위) 기준 정렬 | 칠판 쪽(아래) 기준 정렬 |

두 사람이 서로 마주 보고 있으므로 왼쪽·오른쪽뿐 아니라 앞뒤도 반대라는 점에 착안했다. 좌우만 뒤집으면
칠판이 계속 같은 쪽(위)에 있어서 교사 시점이 되지 않는다.

### PNG로 저장

- `html2canvas`로 좌석표(제목 + 칠판 + 좌석)만 캡처해서 PNG로 내려받는다.
- 화면에 보이는 카드는 넓어도, **저장되는 이미지는 실제 좌석 배치 너비에 맞춰 줄어든다**
  (`.export-tight` 클래스를 캡처 직전에만 붙였다 뗀다 — 화면 레이아웃 자체는 안 건드린다).
- 파일명에 학생용/교사용 구분, 날짜·교시, 학년/반이 들어간다
  (예: `좌석표_학생용_2026-09-08_3교시_1학년2반.png`).

## ③ 기술 메모

- **`index.html` 한 파일**로 완성. 외부 의존성은 Google Fonts(Do Hyeon·Gowun Dodum·JetBrains Mono)와
  `html2canvas`(PNG 저장용, cdnjs)뿐이다.
- **`localStorage`만 쓰고 서버로 아무것도 보내지 않는다.** 좌석 배치·반별 고정석/인원수는 이 브라우저
  안에만 저장된다 — 다른 기기·다른 브라우저에서 열면 처음부터 다시 설정해야 한다.
- 원래 claude.ai 아티팩트(`db`/`downloads` 런타임 capability)용으로 먼저 만들었던 코드라
  `window.claude` 유무를 감지하는 분기가 남아 있다. GitHub Pages 등 일반 브라우저에서는
  `window.claude`가 없으므로 항상 `localStorage` 경로로 동작한다 — 정상이다.
- 색상 테마는 노란색/골드 계열(`--accent` 등 CSS 변수), 라이트/다크 모드 모두 대응.

## ④ 알려진 제한

- 학생 이름이 아니라 **출석번호**만 다룬다(개인정보 최소화).
- 좌석 배치·고정석 정보가 **기기/브라우저 간에 동기화되지 않는다** — 교사가 여러 컴퓨터(교무실 PC,
  컴퓨터실 PC 등)에서 쓰면 각각 따로 설정해야 한다. 필요해지면 내보내기/가져오기(JSON) 기능을 추가할
  수 있다.
